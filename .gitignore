# secrets & local state — never publish
.env
*.keys.json
auth.json
.admin-key.local
# python / macos noise
__pycache__/
*.pyc
.DS_Store

# playwright persistent profile (if ever placed in-repo)
browser/
