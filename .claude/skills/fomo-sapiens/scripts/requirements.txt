# Fomo Sapiens Python deps
curl_cffi>=0.7      # browser TLS impersonation — required; fomo's Cloudflare blocks plain clients (HTTP 430)
solders>=0.21       # solana transaction signing (swap.py execute)
eth-account>=0.11   # EVM userOp + EIP-7702 signing (swap_evm.py)
eth-abi>=5.0        # ABI encoding for userOp hash / executeBatch calldata
rlp>=4.0            # EIP-7702 authorization encoding
# optional — automated login (scripts/login.py); after install also run: playwright install chromium
# playwright>=1.44
